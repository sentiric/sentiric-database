# 🧠 AI SYSTEM INSTRUCTIONS FOR SENTIRIC-DATABASE
1. **Teknoloji:** Saf SQL (PostgreSQL lehçesi).
2. **Single Responsibility:** Bu repo SADECE şema tanımlarını (DDL: `CREATE TABLE`, `CREATE INDEX`, `CREATE TRIGGER`) barındırır.
3. **Yasaklar:** `INSERT` komutları (Veri/Seed) KESİNLİKLE YASAKTIR. Tüm başlangıç verileri `sentiric-config` reposunda bulunmalıdır.
4. **İzolasyon:** Her mikroservisin veritabanı farklı klasörlerdedir. Servisler arası `FOREIGN KEY` kullanımı mimari bir ihlaldir.